{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {<% if (includeRequireJS) { %>
    "requirejs": "~2.1.5",
    "requirejs-text": "~2.0.5",<% } %>
    "themproject": "2.0.0-beta"<% if (templateFramework === 'handlebars') { %>,
    "handlebars": "~1.0.0"<% } %>
  },
  "devDependencies": {}
}